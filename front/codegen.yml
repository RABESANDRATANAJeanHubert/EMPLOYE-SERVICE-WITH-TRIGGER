overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
generates:
  src/graphql/types.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
